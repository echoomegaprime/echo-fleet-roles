interface:
  display_name: "ECHO CLI Build Power"
  short_description: "Drive durable autonomous command-line builds"
  default_prompt: "Use $echo-cli-build-power to execute this role end to end with live ECHO state and proof."
