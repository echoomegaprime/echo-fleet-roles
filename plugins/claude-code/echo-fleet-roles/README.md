# ECHO Fleet Roles for Claude Code

This plugin provides the same 30 ECHO role skills and role-switching control skill as the Codex powerpack. Install it from the repository's Claude marketplace, restart once for discovery, then use `echo-role` to select roles without opening additional terminals. Roles specialize workflow; they do not expand host authority or permissions.
